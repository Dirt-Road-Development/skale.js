{
  "name": "@dirtroad/skale",
  "common": {
    "funding": [
      {
        "type": "individual",
        "url": "https://www.buymeacofee.com/thegreataxios"
      }
    ]
  },
  "private": true,
  "repository": {
    "type": "git",
    "url": "git://github.com/Dirt-Road-Development/skale.js.git"
  },
  "version": "0.1.0",
  "description": "The monorepo package for SKALE Netowrk Abstractions",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build ./tsconfig.package.json",
    "dev": "nodemon --watch \"./packages/**/*.*\" --exec npm run build",
    "publish": "npm publish --workspaces --access public",
    "test": "mocha -r ts-node/register tests/**/*.ts",
    "test:watch": "mocha -r ts-node/register tests/**/*.ts --watch --watch-files test",
    "version": "auto-changelog -p && git add CHANGELOG.md"
  },
  "author": "Sawyer Cutler",
  "license": "MIT",
  "dependencies": {
    "@dirtroad/skale": "./packages/skale"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "auto-changelog": "^2.4.0",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.9.3"
  },
  "workspaces": [
    "packages" 
  ]
}
